diff --git a/node_modules/@rarimo/rarime-rn-sdk/build/Freedomtool.js b/node_modules/@rarimo/rarime-rn-sdk/build/Freedomtool.js
index 2719106..375ab75 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/build/Freedomtool.js
+++ b/node_modules/@rarimo/rarime-rn-sdk/build/Freedomtool.js
@@ -149,16 +149,31 @@ export class FreedomTool {
             // votes mask
             answers.map((v) => 1 << Number(v)),
             // User payload: (nullifier, citizenship, identity_creation_timestamp)
+            // TD1 main.nr returns tmp.0..tmp.9 (10 query outputs) before passthroughs;
+            // TD3 returns tmp.0..tmp.8 (9 outputs). Citizenship is the last element of
+            // the query tuple — index 9 for TD1, index 8 for TD3 — but the SDK reads
+            // pub_signals[5]/[6] which corresponds to the *citizenship* field within
+            // the query tuple (5th in TD1, 6th in TD3). See main.nr in the
+            // passport-zk-circuits-noir repo.
             [
                 "0x" + queryProof.pub_signals[0],
-                "0x" + queryProof.pub_signals[6],
+                "0x" + queryProof.pub_signals[passport.dataGroup1.length === 95 ? 5 : 6],
                 identityCreationTimestamp,
             ],
         ]);
         const smtProof = await rarime.getSMTProof(passport);
-        const txCallData = idCardVoting.contractInterface.encodeFunctionData("executeTD1Noir", [
+        // current_date passthrough: TD1 has 10 query outputs + event_id + event_data +
+        // id_state_root + selector → current_date at index 14. TD3 has 9 query outputs
+        // → current_date at index 13.
+        const isTD3 = passport.dataGroup1.length === 93;
+        const currentDateIdx = isTD3 ? 13 : 14;
+        // TD1 uses executeTD1Noir on IDCardVoting; TD3 uses executeNoir on
+        // BioPassportVoting (sendVoteContractAddress determines which contract).
+        // Both have the same 4-arg signature.
+        const fnName = isTD3 ? "executeNoir" : "executeTD1Noir";
+        const txCallData = idCardVoting.contractInterface.encodeFunctionData(fnName, [
             smtProof.root,
-            "0x" + queryProof.pub_signals[14],
+            "0x" + queryProof.pub_signals[currentDateIdx],
             userDataEncoded,
             "0x" + queryProof.proof,
         ]);
@@ -182,7 +197,9 @@ export class FreedomTool {
             body: JSON.stringify(sendVoteRequest),
         });
         if (!sendVoteResponse.ok) {
-            throw new Error(`HTTP error ${sendVoteResponse.status}`);
+            const errBody = await sendVoteResponse.text().catch(() => '(unreadable)');
+            console.error(`[SDK] sendProposalRequest failed: ${sendVoteResponse.status} ${sendVoteResponse.statusText}\nURL: ${this.config.api.votingRelayerUrl}/integrations/proof-verification-relayer/v3/vote\nRequest: ${JSON.stringify(sendVoteRequest)}\nResponse: ${errBody}`);
+            throw new Error(`HTTP error ${sendVoteResponse.status}: ${errBody}`);
         }
         const sendVoteResponseParsed = await sendVoteResponse.json();
         return sendVoteResponseParsed.data.id;
diff --git a/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.d.ts b/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.d.ts
index 9655e7b..6940a23 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.d.ts
+++ b/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.d.ts
@@ -22,6 +22,7 @@ export interface RarimeConfiguration {
 }
 export declare class Rarime {
     private config;
+    static registerBundledCircuit(name: string, jsonObjectOrString: object | string): void;
     constructor(config: RarimeConfiguration);
     getPassportInfo(passport: RarimePassport): Promise<[
         StateKeeper.PassportInfoStructOutput,
diff --git a/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.js b/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.js
index fd0ba76..2dea7f0 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.js
+++ b/node_modules/@rarimo/rarime-rn-sdk/build/Rarime.js
@@ -11,8 +11,19 @@ import { toPaddedHex32, wrapPem } from "./utils";
 import { Poseidon } from "@iden3/js-crypto";
 import { Time } from "@distributedlab/tools";
 const ZERO_BYTES = new Uint8Array(64);
+// Registry of pre-bundled circuit bytecode JSON strings, keyed by circuit name.
+// Populated by the host app at startup via Rarime.registerBundledCircuit() so the
+// SDK can use circuits that aren't published on Rarimo's GCS bucket — currently
+// only the TD3 (passport) query circuit, self-compiled from
+// rarimo/passport-zk-circuits-noir and shipped in app assets.
+const bundledCircuits = {};
 export class Rarime {
     config;
+    static registerBundledCircuit(name, jsonObjectOrString) {
+        bundledCircuits[name] = typeof jsonObjectOrString === "string"
+            ? jsonObjectOrString
+            : JSON.stringify(jsonObjectOrString);
+    }
     constructor(config) {
         if (config.userConfiguration.userPrivateKey.startsWith("0x")) {
             config.userConfiguration.userPrivateKey =
@@ -41,19 +52,20 @@ export class Rarime {
         if (activeIdentity === ZERO_BYTES_STRING) {
             return DocumentStatus.NotRegistered;
         }
-        if (activeIdentity === this.config.userConfiguration.userPrivateKey) {
+        // Chain stores the profileKey (Poseidon hash of the BJJ pubPoint),
+        // not the private key itself. Compare apples to apples.
+        const profileKey = "0x" + RarimeUtils.getProfileKey(this.config.userConfiguration.userPrivateKey);
+        if (activeIdentity === profileKey) {
             return DocumentStatus.RegisteredWithThisPk;
         }
         return DocumentStatus.RegisteredWithOtherPk;
     }
     async registerIdentity(passport) {
-        const passportStatus = await this.getDocumentStatus(passport);
-        if (passportStatus === DocumentStatus.RegisteredWithOtherPk) {
-            throw new Error("This document was registered with other Private Key");
-        }
-        if (passportStatus === DocumentStatus.RegisteredWithThisPk) {
-            throw new Error("This document was registered with this Private Key");
-        }
+        // Throws for RegisteredWithOtherPk / RegisteredWithThisPk removed — the
+        // app layer (components/voting-modal/Step7.tsx) decides the policy.
+        // Dev builds allow rotation so a single passport can be re-tested
+        // after a SecureStore wipe; prod builds block at the app level so a
+        // double-vote isn't possible by silently rotating keys.
         const hashAlgoOID = passport.extractDGHashAlgo();
         const hashAlgo = HashAlgorithm.fromOID(hashAlgoOID);
         const hashLength = hashAlgo.getByteLength();
@@ -72,9 +84,14 @@ export class Rarime {
         return contract.getProof(smtProofIndex);
     }
     async generateQueryProof(queryProofParams, passport) {
-        const circuit = NoirCircuitParams.fromName("query_identity");
+        // TD1 ID-card DG1 = 95 bytes, TD3 passport DG1 = 93 bytes. The published
+        // Rarimo Noir circuits only cover TD1; the TD3 query circuit is self-compiled
+        // and shipped in-app via Rarime.registerBundledCircuit().
+        const isTD3 = passport.dataGroup1.length === 93;
+        const circuitName = isTD3 ? "query_identity_td3" : "query_identity";
+        const circuit = NoirCircuitParams.fromName(circuitName);
         await NoirCircuitParams.downloadTrustedSetup();
-        const byteCode = await circuit.downloadByteCode();
+        const byteCode = bundledCircuits[circuitName] ?? (await circuit.downloadByteCode());
         const profileKey = "0x" +
             RarimeUtils.getProfileKey(this.config.userConfiguration.userPrivateKey);
         const passportInfo = await this.getPassportInfo(passport);
@@ -82,50 +99,25 @@ export class Rarime {
             throw new Error(`profile key mismatch. profileKey = ${profileKey}, passportInfo.activeIdentity = ${passportInfo[0].activeIdentity}`);
         }
         const smtProof = await this.getSMTProof(passport);
-        const isAndroid = Platform.OS === "android";
         const inputs = {
-            event_id: isAndroid
-                ? toPaddedHex32(queryProofParams.eventId)
-                : queryProofParams.eventId, //from input
-            event_data: isAndroid
-                ? toPaddedHex32(queryProofParams.eventData)
-                : queryProofParams.eventData,
-            id_state_root: smtProof.root, //from SMT
-            selector: isAndroid
-                ? toPaddedHex32(queryProofParams.selector)
-                : queryProofParams.selector, //from input
+            event_id: toPaddedHex32(queryProofParams.eventId),
+            event_data: toPaddedHex32(queryProofParams.eventData),
+            id_state_root: smtProof.root,
+            selector: toPaddedHex32(queryProofParams.selector),
             current_date: hexlify(toUtf8Bytes(new Time().format("YYMMDD"))),
-            timestamp_lowerbound: isAndroid
-                ? toPaddedHex32(queryProofParams.timestampLowerbound)
-                : queryProofParams.timestampLowerbound, //from input
-            timestamp_upperbound: isAndroid
-                ? toPaddedHex32(queryProofParams.timestampUpperbound)
-                : queryProofParams.timestampUpperbound, //from input
-            identity_count_lowerbound: isAndroid
-                ? toPaddedHex32(queryProofParams.identityCountLowerbound)
-                : queryProofParams.identityCountLowerbound, //from input
-            identity_count_upperbound: isAndroid
-                ? toPaddedHex32(queryProofParams.identityCountUpperbound)
-                : queryProofParams.identityCountUpperbound, //from input
-            birth_date_lowerbound: isAndroid
-                ? toPaddedHex32(queryProofParams.birthDateLowerbound)
-                : queryProofParams.birthDateLowerbound, //from input
-            birth_date_upperbound: isAndroid
-                ? toPaddedHex32(queryProofParams.birthDateUpperbound)
-                : queryProofParams.birthDateUpperbound, //from input
-            expiration_date_lowerbound: isAndroid
-                ? toPaddedHex32(queryProofParams.expirationDateLowerbound)
-                : queryProofParams.expirationDateLowerbound, //from input
-            expiration_date_upperbound: isAndroid
-                ? toPaddedHex32(queryProofParams.expirationDateUpperbound)
-                : queryProofParams.expirationDateUpperbound, //from input
-            citizenship_mask: isAndroid
-                ? toPaddedHex32(queryProofParams.citizenshipMask)
-                : queryProofParams.citizenshipMask, //from input
+            timestamp_lowerbound: toPaddedHex32(queryProofParams.timestampLowerbound),
+            timestamp_upperbound: toPaddedHex32(queryProofParams.timestampUpperbound),
+            identity_count_lowerbound: toPaddedHex32(queryProofParams.identityCountLowerbound),
+            identity_count_upperbound: toPaddedHex32(queryProofParams.identityCountUpperbound),
+            birth_date_lowerbound: toPaddedHex32(queryProofParams.birthDateLowerbound),
+            birth_date_upperbound: toPaddedHex32(queryProofParams.birthDateUpperbound),
+            expiration_date_lowerbound: toPaddedHex32(queryProofParams.expirationDateLowerbound),
+            expiration_date_upperbound: toPaddedHex32(queryProofParams.expirationDateUpperbound),
+            citizenship_mask: toPaddedHex32(queryProofParams.citizenshipMask),
             sk_identity: "0x" + this.config.userConfiguration.userPrivateKey,
             pk_passport_hash: toPaddedHex32(passport.getPassportKey()),
-            dg1: NoirCircuitParams.formatArray(Array.from(passport.dataGroup1).map((byteValue) => byteValue.toString()), isAndroid),
-            siblings: smtProof.siblings, //from SMT
+            dg1: NoirCircuitParams.formatArray(Array.from(passport.dataGroup1).map((byteValue) => byteValue.toString()), true),
+            siblings: smtProof.siblings,
             timestamp: toPaddedHex32(passportInfo[1].issueTimestamp),
             identity_counter: toPaddedHex32(passportInfo[0].identityReissueCounter),
         };
@@ -161,12 +153,17 @@ export class Rarime {
         return txCallData;
     }
     async generateLiteRegisterProof(hashLength, passport) {
-        const circuit = NoirCircuitParams.fromName("register_light_" + hashLength);
+        // TD3 (passport, DG1=93) and TD1 (ID card, DG1=95) use the same lite-register
+        // circuit shape but a different compile-time DG1 length. The TD3 variants are
+        // self-compiled and bundled in-app — URL fallback won't work since Rarimo
+        // doesn't host them.
+        const isTD3 = passport.dataGroup1.length === 93;
+        const circuitName = (isTD3 ? "register_light_td3_" : "register_light_") + hashLength;
+        const circuit = NoirCircuitParams.fromName(circuitName);
         await NoirCircuitParams.downloadTrustedSetup();
-        const byteCode = await circuit.downloadByteCode();
-        const isAndroid = Platform.OS === "android";
+        const byteCode = bundledCircuits[circuitName] ?? (await circuit.downloadByteCode());
         let inputs = {
-            dg1: NoirCircuitParams.formatArray(Array.from(passport.dataGroup1).map((byteValue) => byteValue.toString()), isAndroid),
+            dg1: NoirCircuitParams.formatArray(Array.from(passport.dataGroup1).map((byteValue) => byteValue.toString()), true),
             sk_identity: "0x" + this.config.userConfiguration.userPrivateKey,
         };
         return circuit.prove(JSON.stringify(inputs), byteCode);
@@ -219,7 +216,9 @@ export class Rarime {
             body: JSON.stringify(verifySodRequest),
         });
         if (!verifySodResponse.ok) {
-            throw new Error(`HTTP error ${verifySodResponse.status}`);
+            const errBody = await verifySodResponse.text().catch(() => "(unreadable)");
+            console.error(`[SDK] verifySodRequest failed: ${verifySodResponse.status} ${verifySodResponse.statusText}\nURL: ${this.config.apiConfiguration.rarimeApiUrl}/integrations/incognito-light-registrator/v1/registerid\nDocument SOD: hash=${verifySodRequest.data.attributes.document_sod.hash_algorithm} sig=${verifySodRequest.data.attributes.document_sod.signature_algorithm} dg15Len=${verifySodRequest.data.attributes.document_sod.dg15.length} sodLen=${verifySodRequest.data.attributes.document_sod.sod.length}\nResponse: ${errBody}`);
+            throw new Error(`HTTP error ${verifySodResponse.status}: ${errBody}`);
         }
         return verifySodResponse;
     }
diff --git a/node_modules/@rarimo/rarime-rn-sdk/build/RarimePassport.js b/node_modules/@rarimo/rarime-rn-sdk/build/RarimePassport.js
index c63669b..fe870a5 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/build/RarimePassport.js
+++ b/node_modules/@rarimo/rarime-rn-sdk/build/RarimePassport.js
@@ -95,35 +95,30 @@ export class RarimePassport {
     }
     getMRZData() {
         const mrz = DG1.load(this.dataGroup1);
-        /**
-         * Example of MRZ String
-         *
-         * IDUTO<<<<<<<<<<<<<<<<<<<<<<<<<<
-         * 1234567897UTO9001019M3001018<<
-         * JOHN<<DOE<<<<<<<<<<<<<<<<<<<
-         *
-         */
+
+        if (mrz.length >= 88 && mrz.length < 90) {
+            // TD3 passport (2 lines x 44 chars)
+            const documentType = mrz.slice(0, 2);
+            const namesPart = mrz.slice(5, 44);
+            const [lastName = "", firstName = ""] = namesPart.split("<<");
+            const documentNumber = mrz.slice(44, 53);
+            const issuingCountry = mrz.slice(54, 57);
+            const birthDate = mrz.slice(57, 63);
+            const sex = mrz.charAt(64);
+            const expiryDate = mrz.slice(65, 71);
+            return { documentType, issuingCountry, documentNumber, birthDate, sex, expiryDate, lastName, firstName };
+        }
+
+        // TD1 ID card (3 lines x 30 chars)
         const documentType = mrz.slice(0, 2);
         const issuingCountry = mrz.slice(2, 5);
         const documentNumber = mrz.slice(5, 14);
         const birthDate = mrz.slice(30, 36);
-        const sexChar = mrz.charAt(37);
-        const sex = sexChar;
+        const sex = mrz.charAt(37);
         const expiryDate = mrz.slice(38, 44);
         const namesPart = mrz.slice(60);
-        // split by '<<' like in Rust .split("<<")
         const [firstName = "", lastName = ""] = namesPart.split("<<");
-        const result = {
-            documentType,
-            issuingCountry,
-            documentNumber,
-            birthDate,
-            sex,
-            expiryDate,
-            lastName: firstName,
-            firstName: lastName,
-        };
-        return result;
+        return { documentType, issuingCountry, documentNumber, birthDate, sex, expiryDate, lastName: firstName, firstName: lastName };
     }
     getCertificate() {
         const buffer = Buffer.from(this.sod);
diff --git a/node_modules/@rarimo/rarime-rn-sdk/build/RnNoirModule.js b/node_modules/@rarimo/rarime-rn-sdk/build/RnNoirModule.js
index 741f34f..48df8fd 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/build/RnNoirModule.js
+++ b/node_modules/@rarimo/rarime-rn-sdk/build/RnNoirModule.js
@@ -1,4 +1,4 @@
-import * as FileSystem from 'expo-file-system';
+import * as FileSystem from 'expo-file-system/legacy';
 import { default as NoirModule } from './src/NoirModule';
 export class NoirCircuitParams {
     name;
@@ -115,11 +115,24 @@ export class NoirCircuitParams {
 }
 const supportedNoirCircuits = [
     new NoirCircuitParams('query_identity', 'https://storage.googleapis.com/rarimo-store/passport-zk-circuits-noir/id_cards/query_identity_td1.json', 24),
+    // TD3 (passport) query circuit is self-compiled from rarimo/passport-zk-circuits-noir
+    // and bundled in the app at assets/circuits/query_identity_td3.json. URL is unused
+    // because the bytecode is provided via Rarime.registerBundledCircuit() at app startup.
+    new NoirCircuitParams('query_identity_td3', 'bundled://query_identity_td3', 23),
     new NoirCircuitParams('register_light_160', 'https://storage.googleapis.com/rarimo-store/passport-zk-circuits-noir/id_cards/register_lite_160.json', 3),
     new NoirCircuitParams('register_light_224', 'https://storage.googleapis.com/rarimo-store/passport-zk-circuits-noir/id_cards/register_lite_224.json', 3),
     new NoirCircuitParams('register_light_256', 'https://storage.googleapis.com/rarimo-store/passport-zk-circuits-noir/id_cards/register_lite_256.json', 3),
     new NoirCircuitParams('register_light_384', 'https://storage.googleapis.com/rarimo-store/passport-zk-circuits-noir/id_cards/register_lite_384.json', 3),
     new NoirCircuitParams('register_light_512', 'https://storage.googleapis.com/rarimo-store/passport-zk-circuits-noir/id_cards/register_lite_512.json', 3),
+    // TD3 (passport) lite register circuits — self-compiled from
+    // rarimo/passport-zk-circuits-noir/register_identity_light_td1, with
+    // dg1: [u8; 95] swapped for [u8; 93] and the same hash-byte generic.
+    // Bundled in app assets; URLs unused. Same 3 pub_signals as TD1.
+    new NoirCircuitParams('register_light_td3_160', 'bundled://register_light_td3_160', 3),
+    new NoirCircuitParams('register_light_td3_224', 'bundled://register_light_td3_224', 3),
+    new NoirCircuitParams('register_light_td3_256', 'bundled://register_light_td3_256', 3),
+    new NoirCircuitParams('register_light_td3_384', 'bundled://register_light_td3_384', 3),
+    new NoirCircuitParams('register_light_td3_512', 'bundled://register_light_td3_512', 3),
 ];
 export default NoirModule;
 //# sourceMappingURL=RnNoirModule.js.map
\ No newline at end of file
diff --git a/node_modules/@rarimo/rarime-rn-sdk/ios/RnNoirModule.swift b/node_modules/@rarimo/rarime-rn-sdk/ios/RnNoirModule.swift
index dea6423..00490d6 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/ios/RnNoirModule.swift
+++ b/node_modules/@rarimo/rarime-rn-sdk/ios/RnNoirModule.swift
@@ -66,15 +66,18 @@ public class RnNoirModule: Module {
       }
 
       // Generate proof
+      print("[RnNoir] === Attempting circuit.prove() ===")
       do {
         let proof = try circuit.prove(inputsMap, proof_type: "plonk")
 
-        print("Generated proof: \(proof)")
+        print("[RnNoir] Proof generated successfully")
         let hexProof = proof.proof.map { String(format: "%02x", $0) }.joined()
 
         return hexProof
       } catch {
-        print("Error generating proof: \(error)")
+        print("[RnNoir] Prove FAILED: \(error)")
+        print("[RnNoir] Prove error type: \(type(of: error))")
+        print("[RnNoir] Prove error description: \(error.localizedDescription)")
         throw error
       }
     }
diff --git a/node_modules/@rarimo/rarime-rn-sdk/src/Rarime.ts b/node_modules/@rarimo/rarime-rn-sdk/src/Rarime.ts
index 02b4873..cfd61da 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/src/Rarime.ts
+++ b/node_modules/@rarimo/rarime-rn-sdk/src/Rarime.ts
@@ -95,7 +95,10 @@ export class Rarime {
       return DocumentStatus.NotRegistered;
     }
 
-    if (activeIdentity === this.config.userConfiguration.userPrivateKey) {
+    // Chain stores the profileKey (Poseidon hash of the BJJ pubPoint),
+    // not the private key itself. Compare apples to apples.
+    const profileKey = "0x" + RarimeUtils.getProfileKey(this.config.userConfiguration.userPrivateKey);
+    if (activeIdentity === profileKey) {
       return DocumentStatus.RegisteredWithThisPk;
     }
 
@@ -103,16 +106,11 @@ export class Rarime {
   }
 
   public async registerIdentity(passport: RarimePassport): Promise<String> {
-    const passportStatus = await this.getDocumentStatus(passport);
-
-    if (passportStatus === DocumentStatus.RegisteredWithOtherPk) {
-      throw new Error("This document was registered with other Private Key");
-    }
-
-    if (passportStatus === DocumentStatus.RegisteredWithThisPk) {
-      throw new Error("This document was registered with this Private Key");
-    }
-
+    // Throws for RegisteredWithOtherPk / RegisteredWithThisPk removed — the
+    // app layer (components/voting-modal/Step7.tsx) decides the policy.
+    // Dev builds allow rotation so a single passport can be re-tested after
+    // a SecureStore wipe; prod builds block at the app level so a double-vote
+    // isn't possible by silently rotating keys.
     const hashAlgoOID = passport.extractDGHashAlgo();
 
     const hashAlgo = HashAlgorithm.fromOID(hashAlgoOID);
@@ -181,55 +179,30 @@ export class Rarime {
 
     const smtProof = await this.getSMTProof(passport);
 
-    const isAndroid = Platform.OS === "android";
     const inputs = {
-      event_id: isAndroid
-        ? toPaddedHex32(queryProofParams.eventId)
-        : queryProofParams.eventId, //from input
-      event_data: isAndroid
-        ? toPaddedHex32(queryProofParams.eventData)
-        : queryProofParams.eventData,
-      id_state_root: smtProof.root, //from SMT
-      selector: isAndroid
-        ? toPaddedHex32(queryProofParams.selector)
-        : queryProofParams.selector, //from input
+      event_id: toPaddedHex32(queryProofParams.eventId),
+      event_data: toPaddedHex32(queryProofParams.eventData),
+      id_state_root: smtProof.root,
+      selector: toPaddedHex32(queryProofParams.selector),
       current_date: hexlify(toUtf8Bytes(new Time().format("YYMMDD"))),
-      timestamp_lowerbound: isAndroid
-        ? toPaddedHex32(queryProofParams.timestampLowerbound)
-        : queryProofParams.timestampLowerbound, //from input
-      timestamp_upperbound: isAndroid
-        ? toPaddedHex32(queryProofParams.timestampUpperbound)
-        : queryProofParams.timestampUpperbound, //from input
-      identity_count_lowerbound: isAndroid
-        ? toPaddedHex32(queryProofParams.identityCountLowerbound)
-        : queryProofParams.identityCountLowerbound, //from input
-      identity_count_upperbound: isAndroid
-        ? toPaddedHex32(queryProofParams.identityCountUpperbound)
-        : queryProofParams.identityCountUpperbound, //from input
-      birth_date_lowerbound: isAndroid
-        ? toPaddedHex32(queryProofParams.birthDateLowerbound)
-        : queryProofParams.birthDateLowerbound, //from input
-      birth_date_upperbound: isAndroid
-        ? toPaddedHex32(queryProofParams.birthDateUpperbound)
-        : queryProofParams.birthDateUpperbound, //from input
-      expiration_date_lowerbound: isAndroid
-        ? toPaddedHex32(queryProofParams.expirationDateLowerbound)
-        : queryProofParams.expirationDateLowerbound, //from input
-      expiration_date_upperbound: isAndroid
-        ? toPaddedHex32(queryProofParams.expirationDateUpperbound)
-        : queryProofParams.expirationDateUpperbound, //from input
-      citizenship_mask: isAndroid
-        ? toPaddedHex32(queryProofParams.citizenshipMask)
-        : queryProofParams.citizenshipMask, //from input
+      timestamp_lowerbound: toPaddedHex32(queryProofParams.timestampLowerbound),
+      timestamp_upperbound: toPaddedHex32(queryProofParams.timestampUpperbound),
+      identity_count_lowerbound: toPaddedHex32(queryProofParams.identityCountLowerbound),
+      identity_count_upperbound: toPaddedHex32(queryProofParams.identityCountUpperbound),
+      birth_date_lowerbound: toPaddedHex32(queryProofParams.birthDateLowerbound),
+      birth_date_upperbound: toPaddedHex32(queryProofParams.birthDateUpperbound),
+      expiration_date_lowerbound: toPaddedHex32(queryProofParams.expirationDateLowerbound),
+      expiration_date_upperbound: toPaddedHex32(queryProofParams.expirationDateUpperbound),
+      citizenship_mask: toPaddedHex32(queryProofParams.citizenshipMask),
       sk_identity: "0x" + this.config.userConfiguration.userPrivateKey,
       pk_passport_hash: toPaddedHex32(passport.getPassportKey()),
       dg1: NoirCircuitParams.formatArray(
         Array.from(passport.dataGroup1).map((byteValue) =>
           byteValue.toString()
         ),
-        isAndroid
+        true
       ),
-      siblings: smtProof.siblings, //from SMT
+      siblings: smtProof.siblings,
       timestamp: toPaddedHex32(passportInfo[1].issueTimestamp),
       identity_counter: toPaddedHex32(passportInfo[0].identityReissueCounter),
     };
@@ -296,13 +269,12 @@ export class Rarime {
 
     const byteCode = await circuit.downloadByteCode();
 
-    const isAndroid = Platform.OS === "android";
     let inputs = {
       dg1: NoirCircuitParams.formatArray(
         Array.from(passport.dataGroup1).map((byteValue) =>
           byteValue.toString()
         ),
-        isAndroid
+        true
       ),
       sk_identity: "0x" + this.config.userConfiguration.userPrivateKey,
     };
diff --git a/node_modules/@rarimo/rarime-rn-sdk/src/RnNoirModule.ts b/node_modules/@rarimo/rarime-rn-sdk/src/RnNoirModule.ts
index a1aae99..6b6b2c9 100644
--- a/node_modules/@rarimo/rarime-rn-sdk/src/RnNoirModule.ts
+++ b/node_modules/@rarimo/rarime-rn-sdk/src/RnNoirModule.ts
@@ -1,4 +1,4 @@
-import * as FileSystem from 'expo-file-system';
+import * as FileSystem from 'expo-file-system/legacy';
 import {default as NoirModule} from './src/NoirModule';
 
 export type NoirZKProof = {
